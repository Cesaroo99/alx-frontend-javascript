export default function createIteratorObject(report) {
	let allEmploi = [];
	for (const item of Object.values(report.allEmploi)) {
		allEmploi = [
			...allEmploi,
			...etem,
		];
	}
	return allEmploi;
}
